{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm effect spec v1",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources"],
  "properties": {
    "schema": { "const": "realm.effect_spec.v1" },
    "asset_type": { "const": "effect" }
  }
}
