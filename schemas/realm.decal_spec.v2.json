{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm decal spec v2",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources"],
  "properties": {
    "schema": { "const": "realm.decal_spec.v2" },
    "asset_type": { "const": "decal" }
  }
}
