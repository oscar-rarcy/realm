{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm UI asset spec v1",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources"],
  "properties": {
    "schema": { "const": "realm.ui_asset_spec.v1" },
    "asset_type": { "const": "user_interface" }
  }
}
