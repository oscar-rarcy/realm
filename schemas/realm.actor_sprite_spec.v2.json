{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm actor sprite spec v2",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources", "entity"],
  "properties": {
    "schema": { "const": "realm.actor_sprite_spec.v2" },
    "asset_type": { "enum": ["unit", "animal"] }
  }
}
