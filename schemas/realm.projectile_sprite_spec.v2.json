{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm projectile sprite spec v2",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources", "projectile"],
  "properties": {
    "schema": { "const": "realm.projectile_sprite_spec.v2" },
    "asset_type": { "const": "projectile" }
  }
}
