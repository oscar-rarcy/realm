{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm building spec v2",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources", "placement"],
  "properties": {
    "schema": { "const": "realm.building_spec.v2" },
    "asset_type": { "const": "building" }
  }
}
