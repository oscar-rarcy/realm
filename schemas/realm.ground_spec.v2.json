{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Realm ground spec v2",
  "type": "object",
  "required": ["schema", "asset_type", "slug", "name", "render", "paths", "sources"],
  "properties": {
    "schema": { "const": "realm.ground_spec.v2" },
    "asset_type": { "const": "ground" }
  }
}
