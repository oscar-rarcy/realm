# Realm Generated References

This folder is for reference images created by the Realm tileset skill from generated specs and visual context.

These images are not user-supplied source references, not final sprites, and not runtime assets. They are allowed to guide later generation, but accepted game-loadable art still belongs under `assets/tiles/`.

Do not put user-authored references here. User-authored references belong under `art/reference/`.
